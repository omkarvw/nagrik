---
phase: quick
plan: md8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/search.ts
  - src/components/MedicinesList.tsx
  - src/components/CategoryDropdown.tsx
autonomous: true
must_haves:
  truths:
    - "Searching for 'paracetamol' returns medicines containing 'Paracetamol' anywhere in the name"
    - "Search results are ranked by relevance (word boundary matches score higher)"
    - "Category dropdown in MedicinesList is a custom searchable component"
    - "Category dropdown supports keyboard navigation and matches app styling"
  artifacts:
    - path: "src/utils/search.ts"
      provides: "Fuzzy/relevance-based medicine search with scored ranking"
    - path: "src/components/CategoryDropdown.tsx"
      provides: "Reusable searchable dropdown component"
    - path: "src/components/MedicinesList.tsx"
      provides: "Uses custom CategoryDropdown instead of native select"
  key_links:
    - from: "MedicinesList.tsx"
      to: "CategoryDropdown.tsx"
      via: "import and render with category props"
    - from: "SearchAutocomplete.tsx"
      to: "searchMedicines()"
      via: "function call returns scored/ranked results"
---

<objective>
Improve medicine search to return related/fuzzy matches ranked by relevance, and replace the native category dropdown with a searchable custom component.

Purpose: Better UX through smarter search results and consistent UI components.
Output: Enhanced search utility and new searchable category dropdown.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/omkar.wadekar/Desktop/pers-proj/nagrik/src/utils/search.ts
@/Users/omkar.wadekar/Desktop/pers-proj/nagrik/src/components/MedicinesList.tsx
@/Users/omkar.wadekar/Desktop/pers-proj/nagrik/src/components/SearchAutocomplete.tsx
@/Users/omkar.wadekar/Desktop/pers-proj/nagrik/src/types/index.ts

Project Stack:
- React 18 + TypeScript + Vite
- Tailwind CSS with custom color tokens (primary: #FF9933, secondary: #056e00)
- Material Symbols icons
- Static JSON data source
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enhance search utility with fuzzy/relevance scoring</name>
  <files>src/utils/search.ts</files>
  <behavior>
    - Test: searchMedicines(medicines, "paracetamol", 10) returns medicines containing "Paracetamol" in generic name
    - Test: Results with word-boundary matches (e.g., "Paracetamol" at start of word) score higher
    - Test: Results sorted by score descending, limited to specified count
    - Test: Empty query returns empty array
    - Test: No matches returns empty array
  </behavior>
  <action>
    Modify the `searchMedicines` function in `src/utils/search.ts` to:

    1. Remove the early `break` when finding branded matches - collect ALL matches first
    2. Add a scoring function that assigns relevance scores:
       - +10 points: generic name starts with query (e.g., "Paracetamol...")
       - +7 points: generic name has query at word boundary (e.g., "...and Paracetamol...")
       - +5 points: generic name contains query anywhere
       - +3 points: branded equivalent starts with query
       - +1 point: branded equivalent contains query
    3. Sort results by score descending before slicing to limit
    4. Remove duplicate medicines (keep highest scoring match per medicine)

    Keep the existing `normalize()` function and SearchSuggestion type compatibility.

    Example: Searching "para" should return "Paracetamol 500mg" (score 10) before "Aceclofenac and Paracetamol" (score 7).
  </action>
  <verify>
    <automated>npm test -- --run src/utils/search.test.ts 2>/dev/null || echo "Test file does not exist - verify manually with console.log in browser"</automated>
  </verify>
  <done>Search utility returns scored, ranked results; searching for partial terms finds related medicines</done>
</task>

<task type="auto">
  <name>Task 2: Create searchable category dropdown component</name>
  <files>src/components/CategoryDropdown.tsx</files>
  <action>
    Create a new `CategoryDropdown` component that replaces the native `<select>` element:

    1. Props interface:
       ```typescript
       interface CategoryDropdownProps {
         categories: string[];
         selectedCategory: string;
         onSelect: (category: string) => void;
         placeholder?: string;
       }
       ```

    2. Features:
       - Searchable input field showing selected category
       - Dropdown panel with filtered category list
       - Click outside to close
       - Keyboard navigation (ArrowUp/ArrowDown/Enter/Escape)
       - "All" option always visible at top
       - Highlight matching text in results

    3. Styling (match existing design system):
       - Container: `bg-white border border-outline-variant rounded-xl`
       - Focus state: `focus:border-primary focus:shadow-md`
       - Dropdown: `absolute z-50 bg-white rounded-xl shadow-elevation-3 border border-outline-variant`
       - Item hover: `hover:bg-surface-container`
       - Selected item: `bg-primary-container text-primary`
       - Use `material-symbols-outlined` for expand/collapse icon

    4. Implementation notes:
       - Use `useState` for open state, search query, highlighted index
       - Use `useRef` for container and input refs
       - Use `useEffect` for click-outside and keyboard handlers
       - Filter categories based on input text
  </action>
  <verify>
    <automated>grep -q "export function CategoryDropdown" src/components/CategoryDropdown.tsx && echo "Component created"</automated>
  </verify>
  <done>CategoryDropdown component exists with search, keyboard nav, and consistent styling</done>
</task>

<task type="auto">
  <name>Task 3: Integrate custom dropdown into MedicinesList</name>
  <files>src/components/MedicinesList.tsx</files>
  <action>
    Replace the native `<select>` element in `MedicinesList.tsx` with the new `CategoryDropdown`:

    1. Import the new component:
       ```typescript
       import { CategoryDropdown } from './CategoryDropdown';
       ```

    2. Replace lines 48-58 (the native select):
       ```tsx
       <CategoryDropdown
         categories={categories}
         selectedCategory={selectedCategory}
         onSelect={setSelectedCategory}
         placeholder="Filter by category..."
       />
       ```

    3. Adjust wrapper div classes if needed to accommodate the new component's width behavior

    4. Remove unused imports if any

    Ensure the filtered results count still displays correctly below the search/filter row.
  </action>
  <verify>
    <automated>grep -q "CategoryDropdown" src/components/MedicinesList.tsx && ! grep -q "<select" src/components/MedicinesList.tsx && echo "Integration complete"</automated>
  </verify>
  <done>MedicinesList uses CategoryDropdown; no native select elements remain</done>
</task>

</tasks>

<verification>
1. Search for "para" in the main search - should return Paracetamol medicines ranked first
2. Go to "All Medicines" tab - category dropdown should be searchable custom component
3. Type in category dropdown - should filter categories dynamically
4. Keyboard navigation in category dropdown (arrow keys, enter, escape) should work
</verification>

<success_criteria>
- Medicine search returns fuzzy/related matches with relevance scoring
- Category dropdown is custom searchable component (not native select)
- Category dropdown supports keyboard navigation
- Visual styling consistent with app design system
- No TypeScript errors, build passes
</success_criteria>

<output>
After completion, the quick fix is ready for testing.
</output>
